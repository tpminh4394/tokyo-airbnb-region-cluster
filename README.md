# tokyo-airbnb-region-cluster

This is a project in a programming module which require us to dev the k means clustering algorithm from scratch. Then we apply this algorithm to cluster Tokyo airbnb data 
